import QuizClient from '../../components/QuizClient';import {quizzes,tracks} from '../../data/content';
export default function Quizzes(){return <div className="page"><div className="container"><div className="page-head"><div className="eyebrow">Quizzes</div><h1>Find the gap before the interview does.</h1><p className="lead">Short technical and judgment quizzes with explanations. Repeat them after studying instead of memorizing the answer.</p></div><div className="grid grid2">{quizzes.map(q=><QuizClient key={q.id} quiz={q} track={tracks.find(t=>t.id===q.track)}/>)}</div></div></div>}
