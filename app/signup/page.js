"use client";
import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {useAuth} from "../../components/AuthProvider";
export default function Signup(){const {signup,google}=useAuth();const r=useRouter();const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function go(fn){setBusy(true);setError("");try{await fn();r.push("/onboarding")}catch(e){setError(e.message||"Unable to create account.")}finally{setBusy(false)}}return <div className="page"><div className="container center"><div className="card form"><div className="eyebrow">Start free</div><h1>Build your preparation profile.</h1>{error&&<div className="error">{error}</div>}<div className="field"><label>Name</label><input value={name} onChange={e=>setName(e.target.value)} /></div><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={6}/></div><button className="btn primary" onClick={()=>go(()=>signup(name,email,password))} disabled={busy}>{busy?"Creating…":"Create account"}</button><button className="btn" style={{marginTop:8,width:"100%"}} onClick={()=>go(google)} disabled={busy}>Continue with Google</button><p className="muted">Already have an account? <Link href="/login"><b>Log in</b></Link></p></div></div></div>}
