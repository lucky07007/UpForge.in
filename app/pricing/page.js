import Link from 'next/link';
import {plan} from '../../data/content';
export default function Pricing(){return <div className="page"><div className="container"><div className="pricing-head"><div className="eyebrow">UpForge.in</div><h1>One plan. One personal learning account.</h1><p className="lead">A simple ₹49 plan for students who want structured preparation instead of scattered resources.</p></div><div className="pricing-card"><div><span className="tag">Job-Ready Plan</span><h2>{plan.name}</h2><p className="lead">Everything inside UpForge.in for your interview-preparation journey.</p><div className="price huge">₹49 <small>one-time</small></div><Link className="btn primary big" href="/signup">Create my account</Link><p className="small muted">Razorpay integration slot is prepared for the production payment step. Add your Razorpay server key/secret later; learning accounts and the rest of the product do not depend on a payment SDK.</p></div><div className="check-list">{plan.features.map(f=><div key={f}>✓ <span>{f}</span></div>)}</div></div><div className="notice" style={{marginTop:18}}><b>Payment note:</b> This build intentionally keeps payment provider credentials out of the codebase. The checkout boundary is isolated so Razorpay can be added without rebuilding the learning system.</div></div></div>}
