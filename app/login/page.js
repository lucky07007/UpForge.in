"use client";
import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {useAuth} from "../../components/AuthProvider";
export default function Login(){const {login,google}=useAuth();const r=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function go(fn){setBusy(true);setError("");try{await fn();r.push("/dashboard")}catch(e){setError(e.message||"Unable to sign in.")}finally{setBusy(false)}}return <div className="page"><div className="container center"><div className="card form"><div className="eyebrow">UpForge account</div><h1>Welcome back.</h1>{error&&<div className="error">{error}</div>}<div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></div><button className="btn primary" onClick={()=>go(()=>login(email,password))} disabled={busy}>{busy?"Signing in…":"Sign in"}</button><button className="btn" style={{marginTop:8,width:"100%"}} onClick={()=>go(google)} disabled={busy}>Continue with Google</button><p className="muted">New here? <Link href="/signup"><b>Create an account</b></Link></p></div></div></div>}
